interface:
  display_name: "Query Local Database"
  short_description: "Query the local stack PostgreSQL database"
  default_prompt: "Use $query-local-db to inspect the local stack PostgreSQL database safely."
