interface:
  display_name: "Forge Patch"
  short_description: "Fix, verify, record, and open a patch PR"
  default_prompt: "Use $forge-patch to resolve, implement, verify, and document this bug or improvement."

policy:
  allow_implicit_invocation: false
