interface:
  display_name: "Grill Me"
  short_description: "Clarify a plan through focused questions"
  default_prompt: "Use $grill-me to clarify this plan one decision at a time."

policy:
  allow_implicit_invocation: false
