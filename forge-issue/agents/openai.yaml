interface:
  display_name: "Forge Issue"
  short_description: "Implement one scoped change in an isolated checkout"
  default_prompt: "Use $forge-issue to implement this scoped change and leave the diff uncommitted."

policy:
  allow_implicit_invocation: false
