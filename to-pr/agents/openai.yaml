interface:
  display_name: "To PR"
  short_description: "Prepare or create one pull request"
  default_prompt: "Use $to-pr to create or update the current branch pull request in draft mode."

policy:
  allow_implicit_invocation: false
