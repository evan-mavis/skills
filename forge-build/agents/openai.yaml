interface:
  display_name: "Forge Build"
  short_description: "Build, verify, demo, and open a draft PR"
  default_prompt: "Use $forge-build to execute the approved local plan; collect execution mode and optional QA/demo PR evidence choices, then build through a draft PR."

policy:
  allow_implicit_invocation: false
