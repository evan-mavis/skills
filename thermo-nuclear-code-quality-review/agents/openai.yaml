interface:
  display_name: "Thermo-Nuclear Code Quality Review"
  short_description: "Proactively fix structural code-quality issues"
  default_prompt: "Use $thermo-nuclear-code-quality-review to review and fix the current scoped diff."

policy:
  allow_implicit_invocation: false
