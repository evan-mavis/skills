interface:
  display_name: "Deslop"
  short_description: "Remove mechanical AI slop from a scoped diff"
  default_prompt: "Use $deslop to remove mechanical AI slop from the current scoped uncommitted diff."

policy:
  allow_implicit_invocation: false
