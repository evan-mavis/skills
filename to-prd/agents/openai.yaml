interface:
  display_name: "To PRD"
  short_description: "Turn approved context into an agent PRD in the specs repo"
  default_prompt: "Use $to-prd to create an agent-first PRD in the specs repo from this context."

policy:
  allow_implicit_invocation: false
